import React from "react";

const Stats = () => {
  return (
    <footer className="stats">
      <em>👜 You have X items on your lilst, and you already packed X (X%)</em>
    </footer>
  );
};

export default Stats;
